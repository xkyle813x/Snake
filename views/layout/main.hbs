<!DOCTYPE html>
<html>

<head>
	<title>Snake</title>
	<meta charset="UTF-8">
	<link href="style.css" rel="stylesheet">
</head>

<body>
	<div id="app">

		<div id="sidebar">
			<div id="loginbar">
				<script>
					function getCookie(cname) {
						var name = cname + "=";
						var decodedCookie = decodeURIComponent(document.cookie);
						var ca = decodedCookie.split(';');
						for (var i = 0; i < ca.length; i++) {
							var c = ca[i];
							while (c.charAt(0) == ' ') {
								c = c.substring(1);
							}
							if (c.indexOf(name) == 0) {
								return c.substring(name.length, c.length);
							}
						}
						return "";
					}
					const logBar = document.getElementById('loginbar');
					if (document.cookie.indexOf('username=') == -1) {
						logBar.innerHTML = ' <label for="username"> Username: </label><input id="username" name="username" type="text" size="12"><label for="password"> Password: </label><input id="password" name="password" type="text" size="12"><div id = "centre"><button id="login" class="button"> Log In </button><button id="signUp" class="button"> Sign Up</button><p id = "errMsg"></div>';
					}
					else {
						logBar.innerHTML = '<p id = "LogMessage">' + getCookie("username") + '<br /> High Score: ' + getCookie("highscore") + ' </p><button id="logout" class="button"> Log Out</button><p id = "errMsg"></p>';
					}
				</script>
			</div>
			<a href="/leaderboard" id="leaderboard" class="bar"> Leaderboards </a>
			<a href="/editUser" id="editUser" class="bar"> Edit Username or Password </a>
			<a href="/snake" id="snakeButton" class="bar"> <img src="play.jpg" width="100%" height="100%"> </a>
		</div>

		<div id="mainSpace">
			{{{body}}}
		</div>
	</div>
	<script src='/snake.js'></script>
</body>

</html>